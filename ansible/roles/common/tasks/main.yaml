---

- name: install common host packages
  ansible.builtin.apt:
    name:
      - smartmontools # disk health monitoring (beszel)
      - nfs-common # mount.nfs for kubelet in-tree nfs volumes
    state: present
    update_cache: true
  become: true
  tags:
    - common
    - common-packages
